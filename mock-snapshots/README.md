# RTT/1 — Mock Snapshots
This directory contains deterministic mock snapshots for CI testing of the RTT/1 structural engine.

Snapshots are used to:
- validate pipeline stability
- ensure deterministic structural outputs
- support front-end integration tests
- provide reference outputs for developers

Each snapshot corresponds to one RTT/1 layer, plus a full pipeline snapshot.

All snapshots are synthetic, safe, and canon-aligned.

